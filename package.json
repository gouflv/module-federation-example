{
  "name": "module-federation-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "lerna run --parallel dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
